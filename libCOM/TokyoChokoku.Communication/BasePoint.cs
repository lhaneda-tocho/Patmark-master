using System;
namespace TokyoChokoku.Structure
{
    public enum BasePoint
    {
		LB = 0, // 基準点（左下） 
		LM = 1, // 基準点（左中）
		LT = 2, // 基準点（左上）
		CB = 3, // 基準点（中央下）
		CM = 4, // 基準点（中央中）
		CT = 5, // 基準点（中央上）
		RB = 6, // 基準点（右下）
		RM = 7, // 基準点（右中）
		RT = 8, // 基準点（右上）
	}
}
